[package]
name = "xfusion-recomp"
version = "0.1.0"
edition = "2024"

[dependencies]
sharpretro-jit = { path = "../sharpretro-jit" }
libc = "0.2"

[features]
# LLVM tier-TOP passthrough (see sharpretro-jit's Cargo.toml for toolchain).
llvm = ["sharpretro-jit/llvm"]
